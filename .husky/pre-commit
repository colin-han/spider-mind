#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 运行 pre-commit 检查..."

# 运行基本的格式化
echo "🎨 格式化代码..."
yarn format:fix

echo "✅ Pre-commit 检查完成！代码已格式化"
