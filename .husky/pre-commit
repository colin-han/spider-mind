#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 运行 pre-commit 检查..."

# 使用 lint-staged 只对暂存区的文件进行格式化和检查
echo "🎨 运行 lint-staged..."
npx lint-staged

echo "✅ Pre-commit 检查完成！代码已格式化并重新暂存"
